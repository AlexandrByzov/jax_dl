---
title: "Имплементация линейной регрессии на Jax"
author: "Александр Бызов"
date: "2025-04-05"
categories: [jax, linear-regression]
comments:
  utterances:
    repo: AlexandrByzov/jax_dl
---

# Упражнения

## Базовая линейная регрессия

Задание: Напиши простую функцию, которая делает предсказание на основе линейной модели ($y = w*x + b$). Напиши функцию, которая считает ошибки (loss function). Используй `jax.grad` для того, чтобы получить функцию, которая считает градиент потери для `w` и `b`.

Решение: Для начала сделаем простую функцию и протестируем ее.

```{python}
import jax

def predict_fn()
```